<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    
</head>
<body>
    <div class="container">
        <nav>
            <%- include ('../partials/nav.ejs') %>
        </nav>

        <h2>Library</h2>
        <button><a href="/home/library/new">new</a></button>

        <div class="sheets_container">
            <% for (let sheet of sheets) { %>
                <div class="card" style="border: solid black;">
                    <h4><a href="/home/library/<%= sheet._id %>"><%= sheet.title %></a></h4>
                    <p><%= sheet.comp %></p>
                    <form action="/home/library/<%= sheet.id %>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE"/>
                    </form>
                </div>

            <% } %>
            
        </div>
        
    </div>
</body>
</html>